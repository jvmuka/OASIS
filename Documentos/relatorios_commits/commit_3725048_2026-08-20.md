# Relatório de Alterações de Commit

## 1. Identificação
- **Hash do Commit**: `3725048`
- **Branch**: `corrections`
- **Autor**: Jones
- **Data e Horário**: 2026-08-20 15:09 (UTC-3)

---

## 2. Objetivo
Implementar a infraestrutura completa de conteinerização do ecossistema **OASIS** utilizando **Docker** e **Docker Compose**, permitindo a execução unificada do Banco de Dados (PostgreSQL 16), Backend (NestJS) e Frontend (React/Vite com Nginx) com isolamento, redes internas e persistência de dados.

---

## 3. Resumo das Alterações

### Camadas Impactadas
- `raiz`: Criação do arquivo de orquestração `docker-compose.yml`.
- `backend`: Criação do `Dockerfile` multi-stage e `.dockerignore`.
- `frontend`: Criação do `Dockerfile` multi-stage com Nginx, arquivo de configuração `nginx.conf` e `.dockerignore`.
- `banco`: Ajuste dos scripts `02_tabelas.sql` e `oasis_banco_completo.sql` com as colunas `antecedencia_minima_horas` e `imagem_url`.
- `Documentos`: Criação do guia de execução em contêineres `Documentos/guia_docker.md`.

### Arquivos Criados / Modificados
- `docker-compose.yml` (Novo)
- `backend/Dockerfile` (Novo)
- `backend/.dockerignore` (Novo)
- `frontend/Dockerfile` (Novo)
- `frontend/nginx.conf` (Novo)
- `frontend/.dockerignore` (Novo)
- `Documentos/guia_docker.md` (Novo)
- `banco/02_tabelas.sql` (Modificado)
- `banco/oasis_banco_completo.sql` (Modificado)

---

## 4. Detalhamento Técnico

1. **Orquestração com Docker Compose**:
   - Serviço `banco`: Imagem `postgres:16-alpine`, exposta na porta `5433:5432`, com volume nomeado `oasis_pgdata` e carga automática do DDL/DML via `/docker-entrypoint-initdb.d/`.
   - Serviço `backend`: Compilação em `node:20-alpine`, execução com `node dist/main.js` na porta `3000:3000` e volume `oasis_uploads`.
   - Serviço `frontend`: Compilação dos assets com Vite e distribuição via servidor web `nginx:alpine` na porta `5173:80`.
   - Rede interna: `oasis-net` (`bridge`) permitindo comunicação direta por nomes de domínio (`backend:3000`, `banco:5432`).

2. **Servidor Web e Roteamento SPA (Nginx)**:
   - Configurado proxy reverso para `/api/` e `/uploads/` apontando para o backend.
   - Suporte ao roteamento do React Router sem erro 404 via diretiva `try_files $uri $uri/ /index.html;`.
   - Compressão Gzip e cabeçalhos de cache de assets estáticos ativados.

---

## 5. Procedimento de Validação
- `docker compose up -d --build` executado com sucesso no ambiente.
- Contêineres `oasis-banco`, `oasis-backend` e `oasis-frontend` ativos e operacionais.
- Aplicação acessível e validada em `http://localhost:5173`.
